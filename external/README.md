# External

This folder is reserved for third-party code that is intentionally vendored into the repository.

Default policy: **do not vendor external code**. Prefer linking or pinning dependencies in configuration files.

If code is vendored here, include:

- upstream URL
- upstream commit/tag
- upstream license file
- local notes explaining why vendoring is necessary
